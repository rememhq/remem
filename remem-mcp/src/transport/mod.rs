//! MCP transport layer — stdio, SSE, and polling transports.

pub mod stdio;

// Future transports:
// pub mod sse;       // HTTP Server-Sent Events (v0.2)
// pub mod polling;   // HTTP long-polling (v0.2)
